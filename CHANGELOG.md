# 更新日志

## 0.1.0 - 2026-08-01

- 首次发布 Minecraft 1.21.1 / NeoForge 21.1.244 版本。
- 加入 3D 熔核锻台、锻造日志界面、服务端概率预览和约 20 tick 动画。
- 完善“熔核锻造日志”界面：扩大分区、完整概率滚动列表、可拖动滚动条、词条 Attribute 悬停详情及预览上限警告。
- 修正动态锻锤复合旋转导致的穿模，改为锤头朝下的竖直动力锻锤，并加入抬起、接触、回弹和复位动画。
- 新增独立 `Super Reforge` 创造模式页签，按进度排列重铸台与三种媒介，并隐藏动画专用锤子。
- 加入 ID + 随机种子的动态词条解析；数据包更新后旧物品自动读取最新名称、范围及运算。
- 支持多物品类型合并、固定/范围值、三个 Attribute operation 和独立槽位。
- 加入三种默认媒介、8 个品质等级、近战/远程/Curios 共 24 条示例词条。
- 加入可选 Curios 9.x 通用饰品槽兼容。
- 加入可选 KubeJS 2101 定义、谓词与全服持久化进度阶段 API。
- 加入经验等级/点数模式、创造免费策略、自动首词条和失效词条策略。
- 加入 JSON Schema、严格 datapack、JSONC 阅读版和中文 API 文档。
- 修正 1.21.1 资源管理器不接受目录末尾斜杠而导致服务器 datapack 加载中止的问题。
- 新增可直接复制到 KubeJS 的 `examples/kubejs/superreforge_combat_attributes.js`：用一份 UTF-8 结构化表注册“等级 1”至“等级 8”与 8 组共 64 条战斗词条，并示范 Critical Strike、Ranged Weapon API 和 Curios 的可选 Attribute；头盔、胸甲、护腿、靴子使用独立物品类型、词条 ID 和精确装备槽，选择器对象采用 KubeJS 2101 Rhino 可解析的显式键写法，未安装对应模组时核心功能仍可独立运行。
- 将默认八级显示统一为中文“等级 1”至“等级 8”和英文 “Level 1” 至 “Level 8”，保留原有资源 ID、rank、颜色与旧物品解析兼容。
- 修复 Curios 在创意物品栏或 JEI 构建 tooltip 时传入 null 实体的崩溃：合成客户端上下文只解析显示数据，不再执行服务端物品校正。
- 把重铸界面整理为原版 GUI 控件与面板风格；玩家背包严格使用 176px 面板内的 9 列 × 4 行共 36 个槽位，菜单真实槽与客户端槽框共享同一份几何，避免多出第十列或错位；无有效报价、定义未同步或正在锻造时，原版重铸按钮保持禁用。
- 将最终候选改为“等级 / 词条 / 概率”的单行概率日志，完整保留服务端下发的所有行，并支持滚轮、可拖动滚动条、原版按钮和 Attribute 悬停详情。
- 将动力锻锤改为固定连接枢轴上的 -45° 斜置姿态，抬起、接触、回弹与四向 yaw 共用 renderer 实际消费的变换计划；测试从真实锤子/锻台 JSON 读取全部盒体并以 SAT 校验关键帧无正体积穿模。
- 新增独立的 [`docs/DATAPACK_TUTORIAL.md`](docs/DATAPACK_TUTORIAL.md) 与 [`docs/KUBEJS_TUTORIAL.md`](docs/KUBEJS_TUTORIAL.md)，覆盖文件布局、严格 JSON、重载、定义脚本三选一、相对权重、可选战斗属性、进度阶段与排错流程。
- 修复首次新建/进入世界时必须手动执行 `/reload` 才显示词条前缀和属性的问题：KubeJS 早于 datapack 执行时会暂存完整脚本层并在基础定义就绪后自动发布；客户端退出只清显示镜像，服务端定义改为服务器真正停止后清理，避免内置服务端快照被误删或跨世界残留。
- 新增 `superreforge_curio_progression.js` 纯饰品配置：停用内置 24 条词条与旧战斗池，只让 Curios 任意栏位使用 8 级 × 4 条固定词条；六级强化石严格采用整合包给出的等级权重。现有三种石头作为 1–3 级，并新增可由数据包定义获取方式的强化石 4–6 级物品。
